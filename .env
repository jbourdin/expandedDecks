# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=change_me_in_env_local
# Reverse proxy IPs — set to REMOTE_ADDR behind a load balancer, or specific IPs.
# Leave empty for direct client connections (local dev).
# SYMFONY_TRUSTED_PROXIES=REMOTE_ADDR
###< symfony/framework-bundle ###

###> symfony/routing ###
DEFAULT_URI=https://expanded-decks.wip
###< symfony/routing ###

###> doctrine/doctrine-bundle ###
DATABASE_URL="mysql://expanded_decks:expanded_decks@127.0.0.1:3342/expanded_decks?serverVersion=8.0&charset=utf8mb4"
###< doctrine/doctrine-bundle ###

###> symfony/messenger-webhook ###
# Shared secret for authenticating webhook-pushed messages (e.g. from SQS).
# Leave empty to disable webhook consumption (local dev default).
MESSENGER_WEBHOOK_SECRET=
###< symfony/messenger-webhook ###

###> symfony/session ###
# Sessions are stored in the database (via DATABASE_URL) by default.
# To use Redis instead, override handler_id in config/packages/framework.yaml.
###< symfony/session ###

###> symfony/messenger ###
MESSENGER_TRANSPORT_TRANSACTIONAL_EMAIL_DSN=doctrine://default?auto_setup=0
MESSENGER_TRANSPORT_DECK_ENRICHMENT_DSN=doctrine://default?auto_setup=0
MESSENGER_TRANSPORT_NOTIFICATION_DSN=doctrine://default?auto_setup=0
MESSENGER_TRANSPORT_BORROW_LIFECYCLE_DSN=doctrine://default?auto_setup=0
MESSENGER_TRANSPORT_FAILED_DSN=doctrine://default?queue_name=failed
###< symfony/messenger ###

###> symfony/mailer ###
# Local dev: SMTP to Mailpit. Production examples:
# Amazon SES:  ses+smtp://ACCESS_KEY:SECRET@default?region=eu-west-1
# Postmark:    postmark+api://KEY@default
# Mailgun:     mailgun+api://KEY:DOMAIN@default
# SendGrid:    sendgrid+api://KEY@default
MAILER_DSN=smtp://localhost:1035
MAIL_SENDER=noreply@expanded-decks.wip
MAIL_SENDER_NAME="Expanded Decks"
###< symfony/mailer ###

###> symfony/notifier ###
ADMIN_EMAIL=admin@example.com
###< symfony/notifier ###

###> app/version ###
# Application version — set at build time (git tag or commit hash).
# Locally, override in .env.local or leave as "dev".
APP_VERSION=dev
###< app/version ###

###> sentry/sentry-symfony ###
# Sentry DSN for error tracking. Leave empty to disable.
# Example: https://examplePublicKey@o0.ingest.sentry.io/0
SENTRY_DSN=
# Performance tracing sample rate (0.0 = disabled, 1.0 = 100%).
SENTRY_TRACES_SAMPLE_RATE=0
# Minimum level that triggers the Sentry Logs fingers_crossed handler (error, info, debug…).
# Lowering to "info" makes all logs visible in Sentry even without an error.
SENTRY_LOGS_ACTION_LEVEL=error
###< sentry/sentry-symfony ###

###> friendlycaptcha ###
# Friendly Captcha v2 — https://friendlycaptcha.com
# Obtain credentials at https://app.friendlycaptcha.eu
FRIENDLY_CAPTCHA_SITEKEY=
FRIENDLY_CAPTCHA_API_KEY=
# API endpoint: "eu" (default, GDPR) or "global"
FRIENDLY_CAPTCHA_ENDPOINT=eu
###< friendlycaptcha ###

###> mosaic storage ###
# Mosaic image storage adapter: "local" or "s3"
MOSAIC_STORAGE_ADAPTER=local
# Local adapter: directory relative to project root
MOSAIC_STORAGE_LOCAL_DIR=var/storage/mosaic
# S3 adapter (Scaleway Object Storage or any S3-compatible provider)
SCALEWAY_S3_BUCKET=
SCALEWAY_S3_REGION=
SCALEWAY_S3_ENDPOINT=
SCALEWAY_S3_ACCESS_KEY=
SCALEWAY_S3_SECRET_KEY=
# Public base URL for serving mosaic images (e.g. https://bucket.s3.region.scw.cloud)
MOSAIC_PUBLIC_URL=
###< mosaic storage ###
