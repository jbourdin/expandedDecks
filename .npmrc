# webpack-encore 6.0.0 peer-pins typescript@^5 and sass-loader@^16, which lags
# behind the versions this project uses (typescript 6, sass-loader 17). The
# build and test suite are verified working with those versions; skip npm's
# peer-dependency resolution until Encore updates its peer ranges, then remove
# this file.
legacy-peer-deps=true
