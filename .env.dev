# Default values for the dev environment.
# These override .env and are overridden by .env.local.
MESSENGER_WEBHOOK_SECRET=dev-webhook-secret
# Session uses DATABASE_URL in dev (configured in framework.yaml)
MAIL_SENDER=noreply@expanded-decks.wip
ADMIN_EMAIL=admin@expanded-decks.wip
